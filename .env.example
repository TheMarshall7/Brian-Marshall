# Optional: override the built-in GHL inbound webhook for the offer framework lead form
# (defaults are in src/constants/ghl.ts). In GHL: Automations → workflow → Inbound Webhook → copy URL.
# VITE_GHL_OFFER_WEBHOOK_URL=

# Optional: Stripe / Gumroad checkout URL for Knowledge to Cash Blueprint ($29.97) on /blueprint
# VITE_K2C_CHECKOUT_URL=
